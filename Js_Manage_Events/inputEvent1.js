//When you mouseover the input, an alert must pop.

//Save the input in a variable if you want to use the js mode, in this one i choosed to put the enevt trigger in the html.
const input = document.getElementById("input");

function alertMouseOver(){
    alert('Sahba');
}

// input.onmousemove = alertMouseOver();